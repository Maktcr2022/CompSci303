the code will use a file with the name "ints.txt" as a basis for the assignment. The program will ask the user for input and when given the proper input,
will result in the program performing the specified action.
